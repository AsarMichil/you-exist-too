<script lang="ts">
	const { errors } = $props(); // errors is an object with the field name as the key and the error messages as the values
</script>

<div class="text-red-500 text-sm">
	<!-- {#each errorPairs as errorPair}
		{errorPair[0]}: 
		{errorPair[1]}
	{/each} -->
</div>
